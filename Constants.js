const TEMPLETE_DIR_NAME = `templates`;
const CONFIG_DIR_NAME = `data/supported-devices/devices`;
const MD_OUTPUT_DIR = `./content/devices`;
module.exports = {
    TEMPLETE_DIR_NAME,
    CONFIG_DIR_NAME,
    MD_OUTPUT_DIR
}